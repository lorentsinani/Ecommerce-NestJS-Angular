import { Component } from '@angular/core';

/**
  Create a RolePermissions that displays more detailed information about a selected role. This component could display the role name along with a table of all permissions associated with the role, showing the object name and the permission action for each permission.
 */
@Component({
  selector: 'app-admin-role-permissions',
  templateUrl: './role-permissions.component.html',
  styleUrls: ['./role-permissions.component.scss']
})
export class RolePermissionsComponent {}
